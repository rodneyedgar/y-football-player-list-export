{
  "name": "y-player-list-export",
  "version": "0.1.0",
  "private": true,
  "description": "Chromium Manifest V3 extension that exports Yahoo Fantasy Player List data to CSV, TSV, and JSON.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.31",
    "@types/node": "^24.4.0",
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^2.1.1"
  }
}
